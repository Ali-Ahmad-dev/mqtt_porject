import 'package:flutter/material.dart';

class color {
  Color appBackGroundColor = Color.fromARGB(0xFF, 32, 32, 32);
  Color backgroundColor = Color.fromARGB(0xFF, 46, 46, 46);
  Color TextColor = Colors.white70;
  Color headingTextColor = Colors.white54;
  Color amberAccent = Colors.amber;
  Color greenAccent = Colors.green.shade700;
}
